# clean the workspace so we don't have confounding variables
rm(list=ls())
# Define parameters



# Define timescale and number of counties

# Build arrays for storing model output

# Build matrix

# Setup model output file

# Build main loop
